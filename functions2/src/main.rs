// Define a function named "add" that takes two parameters of type i32 and returns an i32 value
fn add(x: i32, y: i32) -> i32 {
    // Return the sum of x and y
    x + y
}

fn main() {
    // Call the add function and print the result
    println!("Sum: {}", add(5, 3));
}
